node_modules/
out/
dist/
.vite/
.electron-cache/
.vscode/
app.log
*.db
*.db-shm
*.db-wal

# Local environment variables (Supabase keys, etc.). The committed
# `.env.example` documents what's needed.
.env
.env.local

# Supabase CLI local state (project ref, pooler URL, version pins).
supabase/.temp/

# Agent tooling profile — may contain API keys. Never commit.
.openclaude-profile.json
